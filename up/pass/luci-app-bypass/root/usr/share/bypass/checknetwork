#!/bin/sh
[ "$1" = check ] || exit 1
NAME=bypass

log(){
	 echo "$(date +'%Y-%m-%d %H:%M:%S') By Rules     : $*" >> >> /tmp/log/$NAME.log
}

while ! curl -so /dev/null -m 3 www.baidu.com;do
	log "Please Check network ..."
	sleep 1
done
/etc/init.d/$NAME start &
